home:
  title: Log Decorators
  description: Comment-based verbose output, with `cli` and `logger` support.

url: https://logdec.justinsingh.me

template:
  params:
    bootswatch: cosmo
  
reference:
  - contents:
    - output
    - matches("%>>%")
    - list_engines
